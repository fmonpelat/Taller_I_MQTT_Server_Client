[package]
name = "gui_mqtt_client"
version = "0.1.0"
edition = "2018"

# For m1 macs only
# [build]
# target = "aarch64-apple-darwin"

[dependencies]
chrono = "0.4.19"
client= { path= "../client" }
gtk = { git = "https://github.com/gtk-rs/gtk3-rs.git" }
